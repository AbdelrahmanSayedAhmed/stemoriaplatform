{
  "name": "@stemoria/database",
  "version": "0.1.0",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "echo 'Database package build complete (using mock client)'",
    "generate": "prisma generate --schema=./prisma/schema.prisma",
    "postinstall": "echo 'Skipping prisma generate during build'"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}